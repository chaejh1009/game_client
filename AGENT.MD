# 개발 작업 규칙

이 저장소에서 개발 작업을 수행할 때는 아래 순서를 반드시 따른다.

## 1. 작업 시작 전 Git 변경점 확인

작업을 시작하기 전에 다음 항목을 확인한다.

1. `git status --short`로 수정·추가·삭제된 파일과 untracked 파일을 확인한다.
2. tracked 파일은 `git diff`와 필요하면 `git diff --cached`로 직전 커밋(`HEAD`)과 비교한다.
3. untracked 파일은 이름만 확인하고 넘어가지 말고 내용을 열어 현재 작업 및 라우팅 문서와의 관계를 확인한다.
4. 직전 커밋도 확인하여 현재 변경이 어떤 기준에서 시작되었는지 한 번 더 대조한다.
5. 기존 변경은 사용자가 만든 변경으로 간주해 보존한다. 명시적인 요청 없이 되돌리거나 덮어쓰지 않는다.

## 2. 사용자가 직접 수정한 코드의 문서 선반영

작업 시작 시 이미 코드 변경이 있다면, 먼저 Git diff를 기준으로 사용자가 수정한 범위와 대응하는 라우팅 문서를 일치시킨다. 코드의 현재 동작, 책임, 호출 관계가 문서에 정확히 반영된 것을 확인한 뒤 다음 개발 작업을 수행한다.

- 클라이언트 라우팅 진입 문서: `client/docs/client-routing/README.md`
- 파일별 라우팅 문서: `client/docs/client-routing/files/<client 상대경로>.md`
- 런타임 소스나 설정 파일을 추가·이동·삭제한 경우 대응하는 파일별 문서도 함께 추가·이동·삭제한다.
- 현재 코드 변경과 관계없는 문서 내용은 수정하지 않는다.

## 3. 개발 중 문서 범위 유지

라우팅 문서는 실제 코드만을 기준으로 작성한다. 계획, 시도했지만 폐기한 구현, 변경되지 않은 동작은 변경 사항으로 기록하지 않는다. 파일별 문서에는 해당 파일이 소유한 책임과 직접 호출 관계만 기록하고, 다른 파일의 내부 구현은 중복해서 설명하지 않는다.

## 4. 개발 완료 후 필수 라우팅 문서 동기화

모든 코드 수정과 검증이 끝난 뒤에는 작업을 종료하기 전에 다음 절차를 추가로 수행한다.

1. `git diff`와 `git status --short`를 다시 확인해 이번 작업에서 실제로 변경된 파일과 내용을 확정한다.
2. 실제 변경된 내용만 대응하는 라우팅 문서에 반영한다.
3. 라우팅 진입 문서의 파일 목록·책임·호출 구조와 각 파일별 문서가 최종 코드와 일치하는지 대조한다.
4. 새 untracked 파일이 누락되지 않았는지 다시 확인한다.
5. 코드와 라우팅 문서가 일치한 상태에서 필요한 검증을 마친다.

라우팅 문서 동기화는 선택 사항이 아니라 모든 개발 작업의 완료 조건이다.

## 5. 검증 완료 후 자동 staging 및 승인 후 commit

코드 수정, 라우팅 문서 동기화, 필요한 검증이 모두 끝났고 문제가 없다면 다음 절차를 수행한다.

1. 이번 작업에 포함되는 최종 변경 파일을 자동으로 `git add`한다. 작업과 관계없는 기존 변경은 임의로 staging하지 않는다.
2. `git status --short`와 `git diff --cached`로 staging된 파일과 내용을 다시 확인한다.
3. staging된 변경 요약과 제안하는 커밋 메시지를 사용자에게 보여 주고 커밋 승인을 요청한다.
4. 사용자의 명시적인 승인을 받은 뒤에만 `git commit`을 실행한다. 승인 전에는 절대로 커밋하지 않는다.
5. 승인을 받으면 제안한 메시지로 자동 커밋하고, 커밋 결과와 커밋 해시를 사용자에게 알린다.

검증 실패나 문서 불일치가 남아 있으면 `git add`와 `git commit`을 진행하지 않고 문제를 먼저 해결한다.
